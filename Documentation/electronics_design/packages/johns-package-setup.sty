%%The package identifies itself and the LaTeX version needed
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{packages/johns-package-setup}[2026/07/12 xcolor define CambridgeBlue]

\RequirePackage{siunitx}
\RequirePackage{hyperref}
\RequirePackage{todonotes}
\RequirePackage{xcolor}

% Defined SI units
\DeclareSIUnit\decade{dec}
\AtBeginDocument{\RenewCommandCopy\qty\SI} % Prefer this definition to the one re-defined by the package physics.

% Hyperref formatting setup
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=cyan,
    }

% General functions
\newcommand\todolist{\listoftodos}

% xcolor Colour definitions
\definecolor{CambridgeBlue}{rgb}{.639 .757 .678}